services:
  trading-bot:
    build: .
    container_name: binance-scalping-bot
    restart: unless-stopped
    env_file:
      - .env
    mem_limit: 512m
    memswap_limit: 512m
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
